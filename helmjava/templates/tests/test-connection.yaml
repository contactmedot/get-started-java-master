apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helmjava.fullname" . }}-test-connection"
  labels:
    {{- include "helmjava.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helmjava.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
